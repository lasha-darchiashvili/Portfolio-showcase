import React from "react";

const RegistrationText = () => {
  return (
    <p className="w-[90%] ">
      A user <span className="text-green">registration</span> with
      <span className="text-green"> validations.</span> The project implements
      both <span className="text-green">GET and POST</span> methods to fetch and
      submit data.
    </p>
  );
};

export default RegistrationText;
