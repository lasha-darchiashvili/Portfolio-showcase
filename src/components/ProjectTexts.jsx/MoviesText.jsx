import React from "react";

const MoviesText = () => {
  return (
    <p className="w-[85%] ">
      A visually appealing <span className="text-green">Frontend</span> movie
      application that <span className="text-green">fetches</span> movie data
      and displays it on the screen as cards. Mostly done with HTML/CSS.
    </p>
  );
};

export default MoviesText;
