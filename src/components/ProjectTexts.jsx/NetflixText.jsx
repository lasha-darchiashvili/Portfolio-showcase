import React from "react";

const NetflixText = () => {
  return (
    <p className="w-[85%] ">
      A fully <span className="text-green">responsive </span>Netflix Clone.
      Authentication, managed with Firebase. Crafted using the{" "}
      <span className="text-green">React</span>, React-Router and React-Context.
    </p>
  );
};

export default NetflixText;
